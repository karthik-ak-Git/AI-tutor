# Development dependencies (for linting and testing)
# Install with: pip install -r requirements-dev.txt

# Linting
flake8>=6.0.0
black>=23.0.0
mypy>=1.5.0
isort>=5.12.0

# Security
bandit>=1.7.5
safety>=2.3.5

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
httpx>=0.25.0

# Note: types-python-dotenv doesn't exist as a package
# python-dotenv has built-in type hints, so no separate types package needed
